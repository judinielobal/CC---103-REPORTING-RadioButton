# CC---103-REPORTING-RadioButton
A radio button is a graphical user interface (GUI) element that allows a user to select exactly one option from a predefined set of mutually exclusive choices. When a new option is selected, the previously selected button in the same group is automatically deselected.
